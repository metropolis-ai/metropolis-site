{
  "name": "metropolis-site",
  "version": "0.0.0",
  "private": true,
  "description": "metropolis marketing site — composed from @metropolis-ai/marketing-site-kit and metropolis's brand tokens.",
  "license": "UNLICENSED",
  "type": "module",
  "packageManager": "pnpm@8.8.0",
  "scripts": {
    "dev": "node scripts/build-blog.mjs && vite",
    "build": "node scripts/build-blog.mjs && vite build && node scripts/prerender.mjs && node scripts/clean-urls.mjs && node scripts/verify-links.mjs",
    "blog": "node scripts/build-blog.mjs",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "encrypt": "node build.cjs",
    "validate:encryption": "node scripts/validate-encryption.cjs",
    "validate:encryption:real": "node scripts/validate-encryption.cjs --real"
  },
  "dependencies": {
    "polis": "git+https://github.com/metropolis-ai/polis.git#cc1af10130323ea3cb9a0d9b03b62e0877982661",
    "@radix-ui/react-label": "^2.1.1",
    "@radix-ui/react-progress": "^1.1.1",
    "@radix-ui/react-separator": "^1.1.1",
    "@radix-ui/react-slot": "^1.1.1",
    "@radix-ui/react-tabs": "^1.1.2",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "lucide-react": "^0.468.0",
    "marked": "^12.0.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "tailwind-merge": "^2.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.17",
    "tailwindcss-animate": "^1.0.7",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  },
  "pnpm": {
    "//": "polis is consumed as SOURCE via a SHA-pinned git dep (see AGENTS.md > Consuming polis). It carries a heavy server-only dependency tree (temporal/nestjs/prisma) that this marketing site never imports; skip those packages' build scripts so install stays fast and doesn't run prisma generate / download native temporal binaries we don't use.",
    "neverBuiltDependencies": [
      "polis",
      "prisma",
      "@prisma/client",
      "@temporalio/core-bridge"
    ]
  }
}
